node_modules
.next
.git
.gitignore
# .env* НЕ копируем в образ: build-time NEXT_PUBLIC_* передаётся через build ARG,
# а runtime-секреты (DB/JWT/GROQ) приходят из docker-compose environment.
# Иначе .env.local пёк localhost:3013 в og:image (инцидент 2026-05-26).
.env*
*.log
# Рабочие каталоги исходников картинок — в образ не нужны (раздаётся только public/)
dvorets-photos
dvorets-images-pack
*-audit.md
docs/handoffs
Dockerfile
docker-compose*.yml
