# Заготовка git commit + PR description

## Commit message

```
feat: добавлены AI-изображения для событий, кружков и hero

- hero.webp + og-cover.webp заменяют placeholder
- 14 карточек: события (6) + кружки (8)
- все изображения сконвертированы в webp q82 (~40-80 KB каждое)
- атрибуция AI добавлена в Footer

Источник: Gemini 2.0 Flash, промты в docs/ai-images-prompts.md
```

## PR description

### Что меняется

Заменены все `placeholder.svg` на AI-сгенерированные изображения в:

- `/` — hero (концертный зал + ансамбль)
- `<meta og:image>` — обложка для шеринга в соцсетях
- `/rent` — 3 карточки залов (большой/камерный/репетиционный)
- `/events` — карточки событий по категориям
- `/clubs` — карточки кружков по направлениям

### Что НЕ меняется

- Логика рендеринга карточек, props, типы — без изменений
- Существующие реальные фото в `public/photos/` (если есть) — остаются
- Размеры компонентов — те же ratio (3:4 / 4:3 / 16:9)

### Где смотреть

| Страница | Что проверить |
|---|---|
| `/` | hero отображается, OG-meta включает og-cover.webp |
| `/rent` | 3 карточки залов с разными интерьерами |
| `/events` | каждая карточка с релевантной картинкой |
| `/clubs` | каждая карточка с релевантной картинкой |
| `/` подвал | строчка «изображения сгенерированы нейросетью» |

### Скриншоты

(приложить /events и /clubs на mobile + desktop)

### Lighthouse

До: hero placeholder.svg ~12 KB
После: hero.webp ~60 KB

LCP не должен ухудшиться — webp с `priority` грузится в первый paint.

### Атрибуция

Footer.tsx — добавлена строка в обоих локалях:

```tsx
{t('footer.aiAttribution')}
// ru: «Иллюстрации для карточек сгенерированы нейросетью на основе текстовых описаний концертной деятельности Дворца. Не являются документальной фотографией.»
// kk: «Карточкаларға арналған иллюстрациялар Мәдениет сарайының концерттік қызметінің мәтіндік сипаттамалары негізінде нейрожелі арқылы жасалған. Деректі фотосуреттер емес.»
```

### Чек-лист перед мержом

- [ ] 16 .webp в `public/dvorets/` присутствуют
- [ ] Размер каждого < 100 KB
- [ ] Все ссылки `placeholder.svg` удалены grep'ом: `git grep placeholder` → 0 результатов в src/
- [ ] `next.config.js` без новых внешних доменов (всё локально)
- [ ] Local build не падает: `pnpm build`
- [ ] OG meta проверена через `https://www.opengraph.xyz/url/https://dvorets-gornyakov.kz`
- [ ] Footer-атрибуция в обоих локалях
