# Handoff 2026-05-26-2 — полная пересборка главной + hero-фикс + i18n залов

**Ветка:** master · **Коммиты:** `b2413d2` (hero-фикс), `acdf2fa` (i18n залов), `c5495f1` (пересборка главной)
**Прод:** пересобран и задеплоен, `https://counting-zero-place-track.trycloudflare.com`

## 1. Пересборка главной /ru и /kk (по твоему контент-аудиту)

Новая структура (сверху вниз):
1. **Hero** + 2 бизнес-CTA: «Афиша» (→/events) и «Аренда залов» (→/rent) — закрыт P1 «hero без CTA»
2. **Главное событие** (featured «Арман») — переименован из «Предстоящие мероприятия "Арман"», закрыт **P0 дубль семантики**
3. **Ближайшие мероприятия** — афиша урезана **8→3** карточки (P1)
4. **Творческие составы** — **12→6** (P2), убраны нерабочие стрелки карусели
5. **Кружки** — 8 направлений, **один** CTA «Все кружки» (убран нижний дубль, P3)
6. **Аренда залов** — NEW teaser, 3 зала из БД (halls-grid, ведут на /rent/[slug])
7. **О дворце** — без изменений
8. **Новости** — NEW, 3 последних published (скрыт если пусто)
9. **Контакты + карта** — NEW тёмный закрывающий блок, Google Maps embed по адресу
- **brand-wall удалён** (избыточен по аудиту)

Серверные счётчики проверены curl: posters=3, collectives=6, halls=3, news=3, CTA «Все кружки»=1.

## 2. hero /kk (правка регресса)
- Первая версия дала регресс /ru (3→4 строки) — **исправлено**: /ru вернул к floor 28px, меньший floor 22px скоуплен на `.hero-h1--kk` (только казахский), мёртвый `<wbr>` убран.
- KK остаётся 4 строки (улучшение с 5), без overflow и без упирания в индикатор.

## 3. i18n залов /kk/events
- `events.location` локализуется через `lib/venue.ts`. Подтверждено тобой ранее — ✓ полностью.

## Просьба — финальный визуальный прогон
**Desktop + mobile (iPhone 390 / Android 360), /ru и /kk:**
1. Новый порядок секций отображается корректно, нет дублей заголовков.
2. **Hero-CTA** видны, кликабельны, на мобильном не ломают вёрстку (на ≤420 они flex 1 1 auto).
3. **Карта в контактах реально загружается** (Google Maps embed) — это главный риск, проверь что не пустой серый блок и нет CSP-ошибки в консоли.
4. Teaser аренды: 3 зала с обложками/вместимостью, ведут на /rent/[slug].
5. Новости: 3 карточки с картинками (fallback если нет image_url).
6. Регресс мобильных фиксов цел: нет горизонтального скролла на 360, бургер 44px, FAB не перекрывает.

Верни: `секция · вьюпорт · OK/проблема · скрин`. Код не трогай.
