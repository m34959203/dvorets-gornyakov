# Handoff 2026-05-25-1 — фиксы P0 после визуального аудита

Контекст: Cowork прогнал визуальный/UX аудит по туннелю. Я (Claude Code) проверил находки
по коду и закрыл блок **P0**. Несколько пунктов аудита оказались ложными (см. ниже).

## Сделано (P0)

| Что | Файл | Суть |
|---|---|---|
| Скроллбар зелёный → коралл | `src/app/globals.css:618-623` | thumb `var(--dg-accent,#E07A4A)`, нейтральный трек, `scrollbar-color` для Firefox. `--dg-accent` скоупнут — поэтому literal-fallback. |
| Чат-виджет зелёный → коралл | `src/components/features/ChatBot.tsx` | floating-button, header, user-bubble, send-button, focus-ring → `#E07A4A`. `aria-label` локализован (Чат-помощник / Чат-көмекші). |
| Логотип обрезан | `src/app/globals.css:872-877` | сетка шапки `1fr auto 1fr` → `auto 1fr auto`: бренд получает натуральную ширину, снят `overflow:hidden`. nav гибко центрируется в средней 1fr. |
| `/login` не на chiaroscuro | `src/app/[locale]/(public)/login/page.tsx` | тёмная карточка `#15152a` + hairline, тёмные инпуты, коралл-кнопка и иконка. |
| `/rules` не на chiaroscuro | `src/app/[locale]/(public)/rules/page.tsx` | тёмный фон, карточка `#15152a`, нумерация коралл. |
| Нет `<h1>` на внутренних | `src/components/layout/DgPageHero.tsx:31` | `<h2 class="h2">` → `<h1 class="h2">` (фиксит events/clubs/news/rent/contacts/resources разом). |
| Нет skip-link | `src/app/[locale]/(public)/layout.tsx` | `<a href="#main">` sr-only + `id="main"` на `<main>`, локализован. |
| **Bonus:** browser-TTS | `src/components/features/ChatBot.tsx` | удалён `speechSynthesis` (нарушал внутреннее правило no-browser-TTS). STT-микрофон оставлен. Полноценный TTS (RU=Gemini/GCloud, KK=ElevenLabs) — отдельной задачей через backend-эндпоинт. |
| Меню плоское/неструктурированное (P0-4) | `sql/018_nav_restructure.sql`, `Header.tsx`, `HeaderClient.tsx`, `globals.css` | Данные nav перестроены в 2 уровня: primary-ряд из 5 (Главная · Мероприятия · Кружки · Аренда залов · Контакты) + группа «Ещё/Тағы» с детьми (О нас · Новости · Ресурсы). Рендер раньше игнорировал `children` — добавлен dropdown (desktop) и вложенность (drawer). Меньше пунктов в ряду = дополнительно снимает тесноту логотипа. |

Образ Docker пересобран, контейнер `dvorets-gornyakov-app-1` (порт 3007) перезапущен, build зелёный.
Все правки подтверждены live на туннеле.

## Аудит ошибся (НЕ чинил)

- **P0-6 «--dg-* токенов нет»** — НЕВЕРНО. `--dg-*` определены `globals.css:801-830` (скоуп `.dg-header/.dg-footer/.dg-drawer/.dg-home` + `.section--light`), используются в about/home/rent/resources. Cowork смотрел `:root` и не увидел скоуп.
- **P1-1 «нет h1 на главной»** — НЕВЕРНО для главной: `page.tsx:185` уже `<h1 class="hero-h1">`. Реально h1 не было только на внутренних (через DgPageHero) — это починено.

## Осталось

- **P1**: EventCard условный CTA (Записаться/Купить билет), контраст sub-CTA `#B4B6C2`, padding стат-блока /about.
- **P2**: /resources иконки+описания, footer соцсети в 1-ю колонку, 2GIS-иконка.
- Реальные мобильные/таблет скрины (390/768) — Cowork не смог через resize; нужен DevTools device toolbar или Playwright matrix.
