# Handoff 2026-05-26-3 — фиксы по экспертному аудиту (P0 + бóльшая часть P1/P2)

**Ветка:** master · **Коммиты:** `b7d9edd`→`5a66591` (7 батчей)
**Прод:** пересобран и задеплоен, `https://counting-zero-place-track.trycloudflare.com`

## Закрыто
- **P0-1** robots.txt/sitemap.xml — теперь актуальный туннель (был `tracy-wal-sep-uses`; корень — `site_settings.site_base_url` в БД, обновлён).
- **P0-2** favicon.ico (`image/x-icon`) + manifest.webmanifest (`application/manifest+json`) + icon/apple-touch-icon в `<head>`.
- **P1-3** опечатка «Дільдебаева» → RU «Дильдебаева», KK «Ділдебаев».
- **P1-4** canonical на главной/resources/rules (на листингах уже был).
- **P1-5** JSON-LD: PerformingArtsTheater (главная), Event (деталь), NewsArticle (деталь), BreadcrumbList (все страницы с крошками).
- **P1-11** security headers (HSTS, nosniff, X-Frame SAMEORIGIN, Referrer-Policy, Permissions-Policy, CSP). **P3-10** убран x-powered-by.
- **P1-12** Cache-Control на /photos,/hero (1д + SWR-неделя).
- **P1-13** image-preload 10→2 (loading=lazy ниже первого экрана).
- **P1-18/19** деталь события: «в календарь» (.ics) + share (TG/WA/копировать) + похожие; деталь новости: share + другие новости.
- **P2-6** убран тройной site-name в title. **P2-7** уникальные title /resources,/rules. **P2-8** og:image на листингах. **P2-15** aria /kk. **P2-22** /rules в дизайн-систему.

## Просьба проверить (desktop + mobile, /ru + /kk)
1. **CSP не сломал сайт** — главный риск. Открой консоль на главной и деталях: нет ли `Refused to ... Content Security Policy`. Особенно проверь, что **Google Maps в контактах всё ещё грузится** (frame-src whitelisted) и GA/Я.Метрика-скрипты (если включены) не блокируются.
2. **favicon** виден на вкладке; `/favicon.ico` отдаёт иконку (не HTML).
3. **Деталь события**: кнопка «Добавить в календарь» скачивает .ics (открой — корректные дата/время/место); share-кнопки TG/WA открывают шаринг, «Копировать ссылку» копирует; блок «Ближайшие мероприятия» (3 карточки).
4. **Деталь новости**: share-row + «Другие новости» (3 карточки).
5. **Титлы вкладок** на /clubs,/news,/rent,/resources,/rules — без тройного «Дворец горняков», читаемые.
6. **/rules** — теперь с hero/крошками/нумерованным списком (не голый список).
7. **Регресс**: мобильные фиксы целы, главная (9 блоков) и карта на месте.

## НЕ сделано (осознанно)
- **P2-9 UUID→slug** — миграция схемы, отложена до переезда на реальный домен (риск/ценность). Сейчас события/кружки по-прежнему по UUID — это работает, просто не идеально для SEO.
- Продуктовые: default locale kk/ru, судьба /resources, дубль «Подробнее», наполнение контента.
- Косметика: fade у topbar-links, топбар на /rent.

Верни: `проверка · результат · скрин`. Особое внимание — CSP-консоль и .ics-файл.
