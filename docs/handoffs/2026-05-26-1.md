# Handoff 2026-05-26-1 — og:image + мобильные фиксы по аудиту

**Ветка:** master · **Коммиты:** `0d2759a` (og), `56d1953` (mobile)
**Прод:** пересобран и задеплоен (docker compose, :3007), URL `https://counting-zero-place-track.trycloudflare.com`

## Что сделано

### og:image (закрыт)
- Корень: `.env.local` (NEXT_PUBLIC_APP_URL=localhost:3013) пёкся в docker-образ → og:image/twitter:image указывали на localhost, шеринг в соцсетях не рендерил обложку.
- Фикс: `NEXT_PUBLIC_APP_URL` теперь build-arg (Dockerfile ARG/ENV до `npm run build`), источник — host `.env`; `.dockerignore` исключает `.env*` из образа.
- Проверено: og:image = `https://counting-zero-place-track.trycloudflare.com/photos/og-cover.jpg` (200, 114KB).
- ⚠️ trycloudflare ephemeral: при смене туннеля обновить `NEXT_PUBLIC_APP_URL` в host `.env` и `docker compose build app`.

### Мобильные фиксы (по твоему отчёту)
- **P0 header overflow (360px):** `@media ≤420` ужимает gap/padding/letter-spacing у `.dg-header-inner`, `.dg-header-right`, `.dg-lang-trigger`, `.dg-brand-name`. Должно убрать горизонтальный скролл документа и вернуть бургер во вьюпорт.
- **P0 тач-таргет:** `.dg-icon-btn` (бургер) 38→44px на ≤768.
- **P1 переключатель `.dg-view`:** `max-width:100%` + `overflow-x:auto` (scroll-safety) + компакт-кнопки на ≤420.
- **P1 chat-FAB:** 56→48px и ближе к углу на мобильном; футеру `padding-bottom:76px` → не перекрывает нижние ссылки.

## ⏳ НЕ сделано (требует решения/отдельного захода)
- **P1 hero /kk:** заголовок ломается на 4-5 строк (длинные казахские слова) — нужно решить (уменьшить font-size на ≤420 или `hyphens`/`word-break`). Не трогал, чтобы не угадывать дизайн.
- **P2 i18n:** на `/kk/events` «ЗАЛ» и «Большой концертный зал» русским — это контент/данные (названия залов в БД, локализация label), не CSS. Отдельный заход.
- **P2 topbar-links:** нет fade-индикатора горизонтального скролла. Косметика.
- **P2 /rent:** topbar-links пропадает (несогласованность с др. страницами).
- **P3:** lang-кнопку можно сжать до иконки; coll-strip без fade.

## Просьба
Перепрогони регресс на тех же 8 страницах (особенно Android 360): подтверди, что (1) горизонтальный скролл ушёл, (2) бургер во вьюпорте и тапается, (3) `.dg-view` не обрезается, (4) FAB не перекрывает футер-ссылки. Скрин до/после по ключевым.
